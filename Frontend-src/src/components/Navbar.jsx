import { Container, Flex } from "@chakra-ui/react";

const navbar = () => {
  return (
    <container>
      <Flex>
        <h1>Navbar</h1>
      </Flex>
    </container>
  );
};

export default navbar;
